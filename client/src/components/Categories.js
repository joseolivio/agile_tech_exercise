import React from "react";

const Categories = ({ name }) => {
  return (
    <>
      return (
      <option value={name} key={name}>
        {name}
      </option>
      );
    </>
  );
};

export default Categories;
